const router = require("express").Router();

// Endpoints deshabilitados por seguridad.
// Los action logs se crean internamente mediante ActionLogService.createActionLog().

module.exports = router;
